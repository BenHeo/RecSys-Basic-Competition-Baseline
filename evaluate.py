import os
import pandas as pd
import numpy as np
from sklearn.metrics import mean_squared_error

def rmse(y, pred):
    
    score = 1
    
    return 

def evaluate(y, pred):
    
    ##### missing/inf value exception logic 필요
    
    rmse_score = rmse(y, pred)
    
    return rmse